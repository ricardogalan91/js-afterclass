let number=3;
let suma=10+number;
let producto=10*number;
let resta=10-number;
let division=10/number;
let remainder=10%number;
let exponencial=10**number;

console.log(suma, producto, resta, division, remainder, exponencial)
//NOTA: REMAINDER se refiere al monto remanente después de la division cuando el primer número no se divide exactamente al valor del otro.
